class Solution(object):
    def addDigits(self, num):
        """
        :type num: int
        :rtype: int
        """
        from math import floor
        if num == 0:
            return 0
        else:
            return int(num - 9 * floor((num-1)/9))
